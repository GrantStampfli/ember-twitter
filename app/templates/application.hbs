<div class="navbar navbar-inverse navbar-fixed-top" role="navigation">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="#">Admit One</a>
    </div>
    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav">
        <li>{{#if session.isAuthenticated }}{{#link-to 'logout'}}Logout{{/link-to}}{{else}}{{#link-to 'login'}}Login{{/link-to}}{{/if}}</li>
        <li>{{#if session.isAuthenticated}}{{#link-to 'profile'}}{{session.username}}{{/link-to}}{{/if}}</li>
        <li>{{#link-to 'tweets'}}View Tweets{{/link-to}}</li>
        <li>{{#if session.isAuthenticated}}{{#link-to 'submit'}}Create Tweet{{/link-to}}{{/if}}</li>
      </ul>
    </div>
  </div>
</div>

<div class="container">
{{outlet}}
</div>
